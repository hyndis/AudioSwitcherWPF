<Window x:Class="AudioSwitcherWPF.AudioOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Height="50"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Width">
    <!-- Anpassa bredd automatiskt efter innehåll -->
    <Border Background="#AA000000" CornerRadius="10" Padding="10" MinWidth="150">
        <TextBlock Name="txtMessage" Foreground="White" FontSize="16" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" 
                   TextWrapping="Wrap"/>
    </Border>
</Window>